version: "3.9"

services:
  app:
    build: .
    container_name: financy_app
    environment:
      NODE_ENV: "production"
      NEXT_PUBLIC_API_BASE_URL: "https://sua-api-aqui"
      NEXT_PUBLIC_AUTH_API_BASE_URL: "https://seu-auth-aqui"
    ports:
      - "3000:3000"
    restart: unless-stopped
