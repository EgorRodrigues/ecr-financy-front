version: "3.9"

services:
  app:
    image: ${IMAGE_NAME}
    container_name: ecr-financy-front
    ports:
      - "3001:3000"
    restart: always
    environment:
      NODE_ENV: production
    networks:
      - ecr-network

networks:
  ecr-network:
    external: true
